import 'package:flutter/material.dart';

class Administrativo extends StatelessWidget {
  const Administrativo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Administrativo')),
      body: const Center(child: Text('Administrativo')),
    );
  }
}
